import {addNumbers,addNumbersAndHalve,objectStringify} from './Functions.js'

const sum = addNumbers(3,5)
console.log(sum)

const halfSum = addNumbersAndHalve(3,5)

objectStringify({id:1, name:"Ducko"})

// we will return to this once we have covered node and npm